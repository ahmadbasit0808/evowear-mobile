# TODO - Product detail navigation & search behavior

- [x] Add `ProductDetail` screen into navigation (so `navigate('ProductDetail')` works)
- [x] Update `components/Home/Card.js` to be tappable and trigger navigation/open detail
- [x] Update `components/Home/Products.js` to show a separate search-results list when `search` is non-empty; tapping result opens `ProductDetail`
- [ ] Run the app and verify:
  - [ ] Tap product card opens ProductDetail
  - [ ] Enter search text shows results list
  - [ ] Tap result opens ProductDetail
